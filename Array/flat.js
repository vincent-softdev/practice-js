// Array.flat() flat all the sub array inside the array, also flat with depth 1

// given the array that has some sub array inside

const array = [1, 2, [3, 4], [5, [6, 7]]];

console.log(array.flat());